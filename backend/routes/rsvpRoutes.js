import { Router } from 'express'; import { createRsvp, listRsvps, updateRsvp, deleteRsvp } from '../controllers/rsvpController.js'; import { protect } from '../middleware/authMiddleware.js'; const router = Router(); router.post('/', createRsvp); router.get('/', protect, listRsvps); router.patch('/:id', protect, updateRsvp); router.delete('/:id', protect, deleteRsvp); export default router
