import { Router } from 'express'; import { getWedding, saveWedding } from '../controllers/weddingController.js'; import { protect } from '../middleware/authMiddleware.js'; import { adminOnly } from '../middleware/adminMiddleware.js'; import { weddingValidation } from '../validators/weddingValidator.js'; import { validate } from '../middleware/validationMiddleware.js'; const router = Router(); router.get('/', getWedding); router.put('/', protect, adminOnly, weddingValidation, validate, saveWedding); export default router
